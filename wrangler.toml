name = "global-data-atlas"
compatibility_date = "2024-01-01"

# Cloudflare Pages 静态站点部署配置
# 部署命令: npx wrangler pages deploy dist --project-name=global-data-atlas
# 或通过 Cloudflare Dashboard → Pages → 连接此目录

[site]
bucket = "./dist"
