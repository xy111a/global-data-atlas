# 全球行政区划数据图谱 · 需求文档与开发规划

> 版本：v2.1（同步当前实现）
> 日期：2026-08-08
> 状态：V1 展示版 + V2 洞察版均已上线（Cloudflare Pages：https://global-data-atlas.pages.dev）；V3 规划见 §9

---

## 1. 项目定位

**一句话**：一个交互式全球行政区划地图，按行政层级（世界 → 国家/中国/美国/日本 → 省/州/都道府县 → 地级市）浏览，每一层挂载公开基础数据（GDP / 人口 / 面积 + 8 项扩展指标），支持热力着色、多年份切换、多区域对比与增长洞察。

**核心价值**：把分散、静态的公开统计数字，组织成可缩放、可下钻、可对比、可看趋势的"数据洞察工具"——从"数据展示"升级为"数据对比/洞察"。

**形态**：单文件 HTML（内联 CSS/JS，零构建）；依赖库本地化，离线可用；按层懒加载大体积边界。已部署 Cloudflare Pages。

---

## 2. 核心概念

| 术语 | 含义 |
|---|---|
| 层级（Level） | world / china / province（中国省级）/ us / japan / province-city，共六种视图状态 |
| 下钻（Drilldown） | 双击进入下一级地图（单击只显示面板，不跳级） |
| 指标（Metric） | 11 项：GDP / 人口 / 面积 + trade 贸易占GDP / health 医疗支出 / edu 教育支出 / life 预期寿命 / gdpcap 人均GDP / unemp 失业率 / internet 互联网普及 / military 军费 |
| 扩展指标（EXT） | World Bank 扩展序列，仅国家层可用（省/州/县自动显示"暂无"） |
| 对比模式（Compare） | 单击地图区域加入对比列表，多区域并排比较当前指标 + GDP/人口/面积 |
| 洞察（Insight） | 趋势图（多年 GDP 序列）+ 较基年增长排行（跟随当前指标） |
| 视图（View） | 2D 平面地图（唯一视图） |

---

## 3. 功能需求（FR）

### FR-1 层级结构
- 世界（214 国，WB 数据）→ 中国 31 省 → 地级市；美国 50 州+DC+9 分区；日本 47 都道府县。
- 中国可完整下钻到地级市（DataV 边界 + 321 城指标）；美国/日本下钻到州/县即终止。
- 港澳台可下钻（港澳 18/8 区，台湾省级面板提示）。

### FR-2 交互触发（V2 拍板：单击/双击分离）
- **单击**：显示该区域数据面板（不下钻）；对比模式下单击加入对比列表。
- **双击**：下钻进入下一级（世界层中国/美国/日本、中国层省份）；无下钻目标的层级双击忽略。
- 260ms 防抖避免双击误触发单击。
- 返回：面包屑可点击（世界 › 中国 › 广东）+ "← 返回中国/世界"按钮（动态文字）+ ESC 键，三者共用 goLevel 逻辑。

### FR-3 数据展示
- 指标切换：11 项指标，按钮由 METRICS 注册表动态生成；地图按当前指标 visualMap 热力着色。
- 年份切换：下拉 2000–2025；国家/州/省为多年序列（缺失回退最近年并诚实标注），城市/日本县为单年快照（标注"约2023"/"2021"）。
- tooltip：名称 + 实际采用年份 + 三项指标 + 较基年增长。
- 右侧面板：名称、三项指标、当前指标增长、来源标签（WB/维基/BEA/日本内阁府/公开整理）、货币标签（按指标动态：GDP→CNY、人均GDP→USD、其余无）。

### FR-4 对比模式
- 开启后单击地图区域加入/移除对比列表；列表显示 当前指标值 + GDP·人口·面积。
- 对比趋势图随指标切换刷新；支持清空。
- 国家/省/州/日本县/城市五类区域均可加入。

### FR-5 洞察
- 国家/省/州：多年 GDP 趋势图（renderTrend）。
- 洞察增长对比图（renderTrendCompare）：较基年增长 TOP 8，跟随当前指标口径（GDP 实际增长用不变价、life 用绝对岁数、其余相对%）。

### FR-6 辅助功能
- 搜索框：国家（含中英文名）/ 中国省/美国州/日本县定位跳级；城市搜索（315 条索引）。
- 排行榜：按当前指标同级排序 TOP 10，点击定位。
- 恢复视图按钮：roam 缩放/平移后一键恢复初始视角（切层自动隐藏）。
- 年份动画（播放按钮）已实现又移除（用户拍板）：保留 redrawByYear 供年份下拉使用。

---

## 4. 数据需求（DR）

### DR-1 数据源分层（现状）
| 层级 | 数据源 | 内容 | 规模 |
|---|---|---|---|
| 国家层 | World Bank API（本地化） | GDP 现价/不变价/PPP、人口、面积 | 214 国 × 2000–2024 完整 |
| 国家扩展指标 | World Bank EXT | trade/health/edu/life/gdpcap/unemp/internet/military | 230 国（含区域聚合键） |
| 中国省 | 维基百科整理（官方公报/五经普修订） | gdpRMB / pop / area 多年序列 | 31 省（2000–2024 + 2025 初核） |
| 中国地级市 | 公开整理近似值 | GDP/人口/面积 | 321 市（约 2023，单年） |
| 美国州 | BEA（SAGDP2 现价）+ Census | GDP 多年序列、人口 2023、面积 | 60 地区（2000–2024） |
| 日本都道府县 | 内阁府県民経済計算 | GDP（2021）/人口（2023）/面积 | 47 县（单年快照） |

### DR-2 数据字段与单位
- **货币统一人民币（CNY）**：国家 GDP 按逐年平均汇率折算；美国州按 BEA 现价 × 逐年汇率；日本县按 1日元≈0.05元近似折算（标注非当年平均汇率）。人均GDP 为 USD(2015 不变价)，面板货币标签随指标动态。
- 人口：人；面积：km²。
- 单年数据明确标注实际年份（城市"约2023"、日本"GDP 2021 / 人口 2023"、国家/州缺失年份回退最近年）。

### DR-3 已知边界
- 云南 5 自治州 adcode 已修正（532800 西双版纳/532900 大理/533100 德宏/533300 怒江/533400 迪庆）。
- 世界地图 217 区域中 202 可着色；15 个空白为 WB 无独立数据的争议/无人/微型属地（tooltip 提示"未纳入 World Bank 序列"）。
- 28 国 WB 有 GDP 但 EXT 无扩展指标（面板显示"—"）。
- 日本县 GDP 折算汇率 0.05 为近似值。

---

## 5. 关键约束与现实边界（必读）

> 不存在一张同时含全球国界+中国各省+浙江各市的世界地图。"放大看到省"本质是检测到聚焦中国 → 加载中国省级 GeoJSON 作为新地图（换图动作）。

> **省/市数据只有中国有完整免费源**；美国/日本有州/县数据但无市下级。公开数据源的边界，不是技术取舍。

> 城市与日本县级数据为单年快照，无多年序列——年份切换在对应层不生效（面板/图例已诚实标注）。

- 坐标系：中国 GCJ-02（DataV），世界 WGS84；不叠底图则无偏移影响。

---

## 6. 非功能需求（NFR）

| 项 | 规格 |
|---|---|
| 兼容性 | Chrome / Safari / Edge 现代版本；2D 视图不依赖 WebGL |
| 离线 | 依赖库（echarts/world/国家/省/市边界/美国/日本）全本地化，file:// 双击可完整浏览 |
| 性能 | 首屏 ≈2.5s（echarts 1MB 最重）；japan.js(3.1MB)/us-states/cn-100000 按层懒加载，首屏 JS ≈3.9MB |
| 风格 | 暗色背景 + 青(#00F0FF)/粉(#FF2E93)/金(#FFD166 负增长) 三色只用于数据语义；响应式断点 1200/1024/768/480 |
| 可读性 | 正文对比度 16.6:1、最小标签 ≥6.4:1；中文字号下限 11px |
| 交付 | 单文件 HTML + 零构建；Cloudflare Pages 部署（dist/ 同步） |

---

## 7. 技术架构

```
┌─ 视图层 ─────────────────────────────┐
│  2D 世界 / 中国省级 / 地级市 / 美国州 / 日本县 │
│  控制栏 / 面包屑 / 信息面板 / 图例 / footer │
├─ 交互层 ─────────────────────────────┤
│  单击显示 · 双击下钻（260ms 防抖）         │
│  对比模式 · 指标切换 · 年份切换 · 搜索/排行 │
├─ 指标层 ─────────────────────────────┤
│  METRICS 注册表（label/fmt/unit/near/    │
│  series/growth/scope）→ 按钮动态生成     │
├─ 数据层 ─────────────────────────────┤
│  reg 统一区域接口（国家/省/州/日本县/城市） │
│  WB 国家 · CN 省序列 · BEA 州 · JP 县     │
│  EXT 扩展指标（8 项，仅国家层）           │
└─ 资源层 ─────────────────────────────┘
```

**架构核心（V2 重构）**：
- **METRICS 指标注册表**：每指标定义 label/fmt/unit/near/series/growth/scope；渲染层全部读注册表。新增指标只需注册 1 条 + 数据 1 文件，按钮自动生成。metricScope 由注册表派生（scope:"world" 即仅国家层）。
- **reg 统一区域接口**：国家/省/州/日本县/城市各暴露 `{get, ext, years, growth, gdpSeries}`；省/州/县对 EXT 指标返回 null 并显示"仅国家层可用"。
- **按层懒加载**：`loadScript()` 动态加载 japan.js/us-states.js/cn-100000.js（首屏不加载）。
- 世界边界 `vendor/world.json` + EN_ALIAS 别名映射（world.json 旧名 → WB 官方名）→ 202/217 可着色。
- 国家数据 `countries_wb.js`（214 国）+ `ext_indicators.js`（230 国 8 指标）。

---

## 8. 当前进度（已完成）

| 项 | 状态 |
|---|---|
| V1 展示版（原型/数据真实化/功能增强/美国下钻/离线部署） | ✅ 已上线 |
| V2 洞察版（METRICS 注册表/统一 reg 接口/趋势洞察/来源标注/按需加载） | ✅ 已上线 |
| 对比模式（多区域并排 + 随指标刷新） | ✅ |
| 11 项指标（+trade/health/edu/life/gdpcap/unemp/internet/military） | ✅ |
| 国家中文名补全（214/214）+ world.json 别名映射 | ✅ |
| 全面审计两轮（数据/逻辑/排版/工程/可信度 5 维） | ✅ 已修复 |
| 单击/双击交互 + 动态返回 + ESC | ✅ |
| F3 日本都道府县层级（47 县下钻） | ✅ |
| 年份动画播放按钮 | ❌ 已移除（用户拍板） |
| 城市多年序列 / 欧盟省级扩展 | 🔶 待做（Phase E） |

---

## 9. 开发路线图

### Phase 0–4 · V1 展示版 ✅
原型 → World Bank 数据 → 搜索/排行榜 → 美国下钻 → 离线化 + Cloudflare Pages 部署。

### Phase D/A/B/C · V2 洞察版 ✅
洞察优先：趋势/增长排行 → 技术债清零 → 部署 → 数据可信度标注。

### Phase F · V3 增量（按用户优先级）
- **F1 年份动画**：播放/暂停（2000→2025 逐年）已实现，**用户拍板移除**——保留 redrawByYear 供年份下拉。
- **F2 更多国家指标** ✅：unemp/internet/military 已接入（11 项）。
- **F3 新增国家层级** ✅：日本 47 都道府县已上线（试点）。
- **Phase E（可选，未做）**：中国城市多年序列（当前单年）；欧盟主要国省级（数据源稀疏）。

---

## 10. 风险与边界

| 风险 | 说明 | 缓解 |
|---|---|---|
| 全球省级数据缺失 | 除中国外无统一免费源 | 明确"层内终止"边界，不编造数据 |
| 数据时效 | 城市/日本县为单年快照 | 面板/图例诚实标注年份（约2023/2021） |
| 扩展指标稀疏 | edu 等部分国家缺年 | extYear 标注实际采用年份；28 国无 EXT 显示"—" |
| 坐标系偏移 | 中国 GCJ-02 vs 世界 WGS84 | 不叠底图则无影响 |

**已拍板决策记录**：洞察优先；Cloudflare Pages 部署；单击/双击分离；返回按钮动态 + ESC；日本作为 F3 试点层级；年份动画移除。

---

## 11. 验收标准（Definition of Done）

- [x] 打开即见 2D 世界地图，无黑屏无报错。
- [x] 单击任意国家 → 面板；双击中国/美国/日本 → 下钻 → 双击省 → 地级市。
- [x] 11 项指标切换热力即时更新；年份 2000–2025 切换（单年层明确标注）。
- [x] 对比模式：五类区域可加入、随指标刷新、趋势图联动。
- [x] 国家层数据来自权威源且覆盖 ≥200 国；来源标签 + footer 汇总。
- [x] 完全离线（file://）核心功能可用；按层懒加载生效。
- [x] 回归套件：offline_test 5/5 + cmp_test 4/4 + ext_test + 语法检查全过。
- [x] 移动端响应式（≤1024 面板全宽、≤768 图表堆叠、指标按钮横向滚动）。
