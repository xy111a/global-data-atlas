# 全球行政区划数据图谱 · 需求文档与开发规划

> 版本：v1.0（草案）
> 日期：2026-08-07
> 状态：Phase 0~3 已完成（原型 + World Bank 数据 + 搜索/排行榜 + 美国州级下钻）；Phase 4~5 待做

---

## 1. 项目定位

**一句话**：一个交互式全球行政区划地图，按行政层级（世界 → 国家 → 省/州 → 市/县）浏览，每一层挂载公开的基础数据（GDP / 人口 / 面积，后续可扩展），数据来自开放的公开数据源。

**核心价值**：把分散、静态的公开统计数字，组织成可缩放、可下钻、可对比的可视化图谱，降低普通人了解"各地基本盘"的门槛。

**形态**：单文件 HTML（内联 CSS/JS，零构建），可直接双击在浏览器打开；依赖库本地化，离线可用。已部署 Cloudflare Pages（https://global-data-atlas.pages.dev）。

---

## 2. 核心概念

| 术语 | 含义 |
|---|---|
| 层级（Level） | 世界 / 国家 / 省·州 / 市·县，共四级 |
| 跳级（Drilldown） | 从当前层进入下一级地图的动作 |
| 指标（Metric） | GDP、人口、面积，可切换并做热力着色 |
| 视图（View） | 2D 平面地图（默认，唯一视图） |

---

## 3. 功能需求（FR）

### FR-1 层级结构
- 四级：世界 → 国家 → 省/州 → 市/县。
- 中国可完整下钻到市/县（边界来自阿里云 DataV GeoAtlas）。
- 其他国家下钻到国家层即终止（无省级免费源，见 §5 约束）。

### FR-2 跳级触发
- **点击进入下一级**（已与用户确认）：点国家 → 进省份；点省份 → 进市/县。
- 层级内的滚轮缩放 / 拖拽只做平移放大，**不触发跳级**。
- 提供面包屑（世界 › 中国 › 浙江）与"返回上一级"按钮。

### FR-3 数据展示
- 支持指标切换：GDP / 人口 / 面积。
- 地图按当前指标做 `visualMap` 热力着色（青 → 粉渐变，越大越粉）。
- 悬浮 tooltip 显示该区域名称 + 三项指标。
- 右侧信息面板：点击区域后展示名称、所属上级、三项指标数值。

### FR-4 视图模式

### FR-5 辅助功能（规划中）
- 搜索框：按国家/省/市名称快速定位并跳级。
- 排行榜侧栏：按当前指标对同级区域排序 Top N。
- 年份选择：固定数据快照年份（如 2023/2024），避免"实时"误导。

---

## 4. 数据需求（DR）

### DR-1 数据源分层
| 层级 | 推荐数据源 | 可得性 | 现状 |
|---|---|---|---|
| 国家层（211 国） | World Bank API（已落地） | 全量免费 | 211 国多年序列（2000–2024）已入库 |
| 中国省/市/县 | 阿里云 DataV GeoAtlas（GCJ-02，已本地化） | 全量免费 | 31 省多年序列 + 321 市指标；省/市边界按需加载 |
| 美国州 | BEA（已落地） | 免费 | 52 州 BEA GDP 多年序列（2000–2024）已接入 |
| 欧盟主要国州/省 | 各国统计署 | 稀疏、非统一 | 未接（Phase E 可选） |

### DR-2 数据字段
每个区域对象至少包含：
```
{ name, adcode/iso, parent, lng, lat, gdp, population, area, year }
```
- `year` 标记数据年份，保证可比性。
- **货币统一为人民币（CNY）**：国家 GDP 由 USD 按 1 USD≈7.2（2023 年均）折算；中国省/市 GDP 原始单位为亿元，统一折算为元（×10⁸）。所有展示使用 ¥ 符号与中文万/亿/万亿制。
- 人口：人（展示用 亿人 / 万人）。
- 面积：km²（展示用 万平方千米 / 平方千米）。

### DR-3 更新与时效
- GDP/人口建议固定年度快照（如 2023），不做伪实时。
- 边界数据跟随 DataV 季度更新，必要时重新拉取。

---

## 5. 关键约束与现实边界（必读）

> 你最初设想的"放大进省、再放大进市"那种 Google Maps 连续缩放感，**技术上是"换图"动作、不会自然发生**——不存在一张同时含全球国界+中国各省+浙江各市的世界地图。所谓"放大看到省"，本质是检测到聚焦到中国 → 加载中国省级 GeoJSON 作为一张新地图。

> **省/市数据只有中国有完整免费源**。因此"丝滑下钻到市/县"实质是**中国专属能力**；全球其他国家只能停在国家一层。这是公开数据源的边界，不是技术取舍。

- 坐标系：中国边界 GCJ-02（DataV），世界 WGS84；纯展示不叠底图则无偏移影响。

---

## 6. 非功能需求（NFR）

| 项 | 规格 |
|---|---|
| 兼容性 | Chrome / Safari / Edge 现代版本；2D 视图不依赖 WebGL |
| 离线 | 依赖库（echarts/world.json/中国边界）本地化；离线可完整浏览 |
| 性能 | 国家层 195 区即时渲染；省/市边界按需加载并缓存 |
| 风格 | 暗色背景 + 青(#00F0FF)#/粉(#FF2E93) 渐变，响应式布局 |
| 交付 | 单文件 HTML，零构建；已部署 Cloudflare Pages |

---

## 7. 技术架构

```
┌─ 视图层 ──────────────────────────┐
│  2D 世界地图 / 中国省级 / 美国州级    │
│  控制栏 / 面包屑 / 信息面板 / 图例   │
├─ 交互层 ──────────────────────────┤
│  点击跳级 · 指标切换 · 对比模式      │
│  搜索 / 排行榜 / 趋势洞察            │
├─ 指标层 ──────────────────────────┤
│  METRICS 注册表（统一指标元数据）     │
│  label/fmt/near/series/growth/unit  │
├─ 数据层 ──────────────────────────┤
│  reg 统一区域接口（国家/省/州/市）    │
│  WB 国家指标 · CN 省级序列 · BEA 州   │
│  EXT 扩展指标（贸易/医疗/教育/寿命）  │
└─ 资源层 ──────────────────────────┘
```

**架构核心（V2 重构）**：
- **METRICS 指标注册表**：每个指标（gdp/pop/area/trade/health/edu/life/gdpcap）定义 label、fmt、unit、near（取某年最近值）、growth。渲染层（地图着色/排行/对比/面板）全部读注册表，新增指标只需注册 1 条 + 数据源 1 个文件，按钮自动生成。
- **reg 统一区域接口**：国家/省/州/市各暴露 `{get, ext, years, growth, gdpSeries}`，取数与增长口径在各 reg 内实现，上层不感知差异。
- 世界边界：`vendor/world.json`（ECharts 官方，已本地化）。
- 中国下钻：运行时 `fetch` DataV `{adcode}_full.json` → `echarts.registerMap` → 重绘。
- 国家数据：`vendor/countries_wb.js`（WB 211 国快照 2000–2024）。
- 扩展指标：`vendor/ext_indicators.js`（WB 179 国 5 指标，仅国家层）。

---

## 8. 当前进度（已完成）

| 项 | 状态 |
|---|---|
| 单文件 HTML 原型 | ✅ |
| 依赖本地化（vendor/） | ✅ |
| 2D 世界地图 + GDP 热力 | ✅ 自测通过 |
| 中国省份下钻（DataV 35 区） | ✅ 自测通过 |
| 浙江地级市下钻（11 市） | ✅ 自测通过 |
| 国家数据真实性 | ❌ 仅 63 国手填近似值 |

---

## 9. 开发路线图（分期）

### Phase 0 · 原型验证 ✅ 已完成
- 单文件原型、本地依赖、2D 世界地图、中国省/市下钻、自测 3 项通过。

### Phase 1 · 数据真实化 ✅ 已完成
- [x] 接入 World Bank API，国家层扩至全量 211 国（GDP/人口/面积，2023），替换手填近似值。
- [x] 国家名与 `world.json` 映射补全（193/211 匹配；剩余 18 个为微型属地/小岛国，world.json 标准边界不含）。
- [x] 中国 31 省 GDP/人口/面积指标（约 2023）。
- [x] 生成 `vendor/countries_wb.json` 本地快照（构建脚本 `build_countries.js` + 命名补丁 `patch_names.js`）。

### Phase 2 · 功能增强 ✅ 已完成
- [x] 搜索框：顶部输入，支持国家/中国省份/美国州，回车或失焦触发，自动跳级。
- [x] 排行榜侧栏：随视图/指标动态更新，同级按当前指标排序 Top 10，点击可跳级定位。
- [x] 面包屑 + 返回交互打磨；tooltip 信息丰富化。
- [x] 年份选择器：UI 控件已就位（2021/2022/2023），当前仅 2023 有完整数据，切换其他年份显示提示。

### Phase 3 · 下钻扩展 ✅ 部分完成
- [x] 美国州级下钻：本地 `vendor/us-states.js`（52 州/特区边界）+ `US_STATES_GDP`（52 州 GDP，美元×7.2 折算人民币），点美国飞入州地图。
- [x] 中国市级 GDP/人口指标接入：`vendor/city-metrics.js`（精选 TOP 58 城市约 2023 近似值，window.CITY_METRICS），市级视图按 GDP/人口热力着色 + tooltip + 排行榜。
- [x] 其他国家"国家层终止"的友好提示（面板说明 + 末匹配搜索提示）。

### Phase 4 · 离线化与部署 ✅ 已完成
- [x] DataV 中国省/市边界完全离线：34 个省级 `_full.json` → `vendor/cn/*.js`（window.CN_GEO / window.CN_PROV），零 fetch。
- [x] **真正的 file:// 完全离线**：双击 HTML 无需网络、无需浏览器 flag，所有功能可用（世界→中国省→市、美国州、搜索、排行榜）。
- [x] 自测验证 4/4 通过（file:// 无 --allow-file-access-from-files）：2D 世界 / 中国省份 / 浙江地级市 / 美国州。
- [x] 部署准备：`dist/` 目录（14MB，HTML + vendor 全量）+ `wrangler.toml`（Cloudflare Pages 配置），可 `npx wrangler pages deploy dist` 一键部署。

### Phase 5 · 体验打磨 🔶 待做
- [x] 移动端适配：CSS 媒体查询（≤768px / ≤480px），窄屏下侧栏堆叠到地图下方、搜索框缩小、按钮触控友好。
- [ ] 性能压测。

---

## 10. 风险与待确认

| 风险 | 说明 | 缓解 |
|---|---|---|
| 全球省级数据缺失 | 除中国外无统一免费源 | 明确"国家层终止"边界，不做伪数据 |
| 数据时效 | GDP/人口逐年变动 | 固定快照年份，标注来源 |
| 坐标系偏移 | 中国 GCJ-02 vs 世界 WGS84 | 不叠底图则无影响；下钻切 DataV 规避 |

**待用户拍板的方向**：
1. 先接 World Bank 实时数据（补最大短板）？
2. 还是先补美/欧州省级下钻？
3. 或先做搜索框 + 排行榜提升可用性？

---

## 11. 验收标准（Definition of Done）

- [x] 打开 HTML 即见 2D 世界地图，无黑屏、无报错。
- [x] 点击任意国家 → 右侧出数据；点"中国"→ 进省份 → 点省份 → 进市。
- [x] 指标切换（GDP/人口/面积）热力即时更新。
- [x] 国家层数据来自权威源且覆盖 ≥ 180 国。
- [x] 断网（除 DataV 边界外）核心功能可用。
