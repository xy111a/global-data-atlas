# 全球数据图谱（Global Data Atlas）· 视觉再设计 Brief

## 产品是什么
交互式全球行政区划数据地图（单页应用，桌面+移动端）。核心交互：世界地图热力着色 → 单击看数据 / 双击下钻（中国省→地级市、美国州、日本都道府县）→ 指标切换（11 项）→ 年份切换（2000-2025）→ 对比模式（多区域并排）→ 趋势洞察/增长排行。

线上：https://global-data-atlas.pages.dev

## 当前视觉语言（保留品牌基因，提升精致度）
- 深空暗色底 `#05060a`，面板 `rgba(12,18,28,0.82)`，边框线 `#13314a`
- 语义色（**仅用于数据语义，勿滥用于装饰**）：青 `#00F0FF`（强调/正值/高值）、粉 `#FF2E93`（色带高值/强调）、金 `#FFD166`（负增长/警示）、灰 `#7f93a8`（弱化）、正文 `#dfeaf5`
- 字体：-apple-system / PingFang SC / Microsoft YaHei
- 数据地图本体（echarts visualMap 青→粉渐变）是主角，UI 需克制不抢戏

## 页面结构（自上而下）
1. **Header 控制栏**：标题「全球数据图谱 · Global Data Atlas」+ 面包屑（世界 › 中国 › 广东，可点击）| 右侧：搜索框、指标按钮组（11 个，可横向滚动）、年份下拉、对比按钮、返回按钮
2. **主区**：左侧 echarts 地图（占满剩余空间，右下角 banner 说明、右上 roam 恢复按钮）| 右侧数据面板（宽 300-380px）：区域名、三项指标大数字、来源标签、趋势图、TOP10 排行
3. **对比模式**：面板顶部对比列表（区域 × 当前指标值 + GDP·人口·面积）
4. **Footer**：数据来源汇总（World Bank / 维基 / BEA / 日本内阁府 等）

## 设计目标（本次再设计重点）
1. **质感升级**：暗色数据可视化产品的精致感——层次（背景/面板/悬浮层）、微光效（边框渐变、阴影、分区高亮）、信息密度与呼吸感的平衡
2. **层级导航更清晰**：面包屑/返回/下钻状态的视觉反馈；当前层级 vs 上级的区分
3. **数据阅读性**：大数字排版（tabular-nums）、指标标签层级、对比列表的可扫读性、排行三列对齐
4. **控件统一**：指标按钮组/下拉/搜索/按钮的视觉一致性（active 态、hover 态、焦点环）
5. **响应式**：桌面 1440 主场景；≤1024 面板转全宽；≤768 图表+面板纵向堆叠；移动端控件触控友好

## 约束
- 只出**视觉/布局设计稿**（配色 token、组件样式、布局示意），不实现业务逻辑
- 保持现有功能结构（元素 id 不变更：chart/panel/metricSeg/yearSel/search/crumb/cmpBtn/backBtn/resetView/banner/trend/rank/compareBox）
- 数据地图 echarts 本体样式可微调（色带/背景/字号）但不改交互
- 输出可作为直接移植进单文件 HTML 的 CSS/结构建议
